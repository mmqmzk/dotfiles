[user]
  name = mmqmzk
  email = mmqmzk@gmail.com
[alias]
  A = add .
  a = add
  aa = add .
  b = branch
  br = branch --remote
  ca = commit -a
  cm = commit
  cma = commit --amend
  cmm = commit -m
  co = checkout
  coa = checkout -- .
  cob = checkout -b
  coc = checkout console
  com = checkout master
  coo = checkout --ours
  cot = checkout --theirs
  cow = checkout work
  cp = cherry-pick
  d = diff
  f = ftech
  f1 = fetch --depth=1
  h = help
  l = log
  lo = log --all --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
  m = merge
  ma = merge --abort
  mc = merge --continue
  mff = merge --ff
  ms = merge --squash
  p = push
  pl = pull
  pl1 = pull --depth=1
  po = push origin --all
  r = remote
  ra = remote add
  rb = rebase
  rba = rebase --abort
  rbc = rebase --continue
  rbm = rebase master
  rbs = rebase --skip
  rr = remote remove
  s = status -sb
  sd = svn dcommit
  sf = submodule foreach --recursive
  sfg = submodule foreach --recursive git
  sh = show
  shh = show --format="%H"
  sr = svn rebase
  su = submodule
  suu = submodule update --init --recursive
[core]
  editor = vim
  autocrlf = false
  quotepath = false
  pager = bash -c '(which diff-so-fancy &>/dev/null && diff-so-fancy "$@" || cat "$@") | less -rFX'
[color]
  pager = true
  ui = true
[text]
  eol=lf
[https]
