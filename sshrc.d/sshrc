#! /usr/bin/env bash

: "/etc/profile" && [[ -f "$_" ]] && source "$_"
: "$HOME/.bashrc" && [[ -f "$_" ]] && source "$_"

# bind "\C-[":character-search-backward
bind "\C-f":beginning-of-line

if command -v vim &> /dev/null; then
  # shellcheck disable=SC2139
  : "$SSHRCD/vimrc" && [[ -f "$_" ]] \
    && alias v="command vim -u '$_'" \
    && export EDITOR="v"
fi

: "$SSHRCD/lsd.sh" && [[ -f "$_" ]] && source "$_"

alias L="less -R"
alias T="tail"
alias H="head -q"
alias h="history"
alias l="command ls --color=auto -lh"
alias la="command ls --color=auto -lha"
alias lsa="command ls --color=auto -A"
alias lss="command ls --color=auto -lhS"
alias lst="command ls --color=auto -lht"
alias t="tail -f"
alias p="ps -ef"
alias g="git"
alias md="mkdir -p"
alias c="cd"
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
alias -- -="cd -"
if command -v ag &> /dev/null; then
  alias agc="ag --color"
  alias G="ag"
  alias GC="ag --color"
elif command -v rg &> /dev/null; then
  alias rg="rg --smart-case"
  alias rgc="rg --smart-case --color=awryas"
  alias G="rg --smart-case"
  alias GC="rg --smart-case --color=awryas"
else
  alias G="grep -Ei"
  alias GC="grep -Ei --color=awryas"
fi

take() {
  mkdir -p "$1" && cd "$1" || return
}

del() {
  local tmp="/tmp"
  for file in "$@"; do
    [[ -e "$file" || -L "$file" ]] && mv --backup=t "$file" "$tmp" && continue
    rm -rf "$file" || true
  done
}

