if [[ -f /etc/profile ]]; then
  source /etc/profile
fi
if [[ -f ~/.bashrc ]]; then
  source ~/.bashrc
fi

stty werase undef
# bind '^[:character-search-backward'

if command -v vim &> /dev/null; then
  export EDITOR="v"
  if [[ -f $SSHRCD/vimrc ]]; then
    alias v="command vim -u $SSHRCD/vimrc"
  fi
fi

alias L="less -r"
alias T="tail"
alias H="head -q"
alias h="history"
alias l="ls -lhA --color=always"
alias t="tail -f"
alias p="ps -ef"
alias g="git"
alias md="mkdir -p"
alias c="cd"
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
alias c-="cd -"

take () {
  mkdir -p $1 && cd $1
}

# export TERM=linux
